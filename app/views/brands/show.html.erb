<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @brand.name %>
</p>

<h3>Vehiculos</h3>
<%#= hacemos un bucle y listamos solo los nombres %>
<ul>
  <% @brand.vehicles.each do |v| %>
    <li><%= v.name %></li>
  <% end %>
</ul>

<p>
  <% @brand.vehicle_ids.inspect  %> <%#inspect para que me muestre si existe algun erros  %>
</p>

<h3>Cliente</h3>
<ul>
  <% @brand.customers.each do |c| %>
    <li><%= c.name %></li>
  <% end %>
</ul>


<%= link_to 'Edit', edit_brand_path(@brand) %> |
<%= link_to 'Back', brands_path %>
